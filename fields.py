from enum import Enum

# didn't decide if should use or not

class field(Enum):
    userid = "USERID"
    state = "STATE"
    problem = "PROBLEM"
    asnwer = "ANSWER"
    addition = "ADDITION"
    subtraction = "SUBTRACTION"
    multiplication = "MULTIPLICATION"
    division = "DIVISION"
    max_sum = "MAX_SUM"
    max_factor = "MAX_FACTOR"
    correct = "CORRECT"
    incorrect = "INCORRECT"
    skipped = "SKIPPED"

